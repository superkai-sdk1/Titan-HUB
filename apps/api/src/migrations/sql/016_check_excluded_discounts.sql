-- Снятые вручную авто/тир-скидки на конкретном чеке. applyAutoDiscounts
-- пересоздаёт авто-скидки при каждом пересчёте позиций, поэтому простое удаление
-- строки вернуло бы скидку. Этот список исключений делает снятие постоянным.
-- Колонка-массив, nullable/default '{}' — добавляется без сканирования таблицы.
ALTER TABLE checks ADD COLUMN IF NOT EXISTS excluded_discount_ids uuid[] DEFAULT '{}';
